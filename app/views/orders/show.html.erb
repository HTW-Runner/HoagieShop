<p>
  <strong>Order:</strong>
  <%= @order.id %><br>
  <%= Customer.find(@order.customer_id).first_name %>
  <%= Customer.find(@order.customer_id).last_name %><br>
  <%= @order.status %><br>
</p>
<p>
  <% @hoagies.each do |hoagie| %>
    <%= hoagie.count %> x
    <%= ::Base.find(hoagie.base_id).name %>
    <% OrderedAdditionally.where(hoagie_id: hoagie.id).each do |extra| %>
      + <%= Ingredient.find(extra.ingredient_id).name %><br>
    <% end %>
  <% end %>
</p>
<%= link_to 'Back to Homepage', home_index_path %>
