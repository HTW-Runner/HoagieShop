<%= form_with(model: [ @review, @review.comments.build ], local: true) do |form| %>
<p>
  <% if session[:customer_id].nil? %>
  <%= link_to 'Login for commenting', login_path %>
</p>
<% else %>
<%= form.label :text %><br>
<%= form.text_area :text %>
<% end %>
<p>
  <%= form.submit %>
</p>
<% end %>
